import PagePlaceholder from "@/components/common/PagePlaceholder"

function DashboardPage() {
  return (
    <PagePlaceholder
      description="A signed-in overview will live here later. For now, the app shell, sidebar, topbar, notifications, and avatar placement are ready."
      title="Dashboard"
    />
  )
}

export default DashboardPage
